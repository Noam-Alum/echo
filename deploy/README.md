# Deployment configuration

CRDs, RBAC, and environment overlays are introduced with the worker vertical slice. This directory is
kept intentionally empty of workload manifests during the foundation phase.

